---
// Tanda 0: navegación estática. En la Tanda 1 pasa a leer la sesión desde
// Astro.locals para mostrar "Entrar" o el menú de usuario según corresponda.
const rutaActual = Astro.url.pathname;

const enlaces = [
  { href: '/', texto: 'Inicio' },
  { href: '/rutina', texto: 'Rutina' },
  { href: '/calendario', texto: 'Calendario' },
  { href: '/progreso', texto: 'Progreso' },
] as const;
---

<header class="border-b sticky top-0 z-10 backdrop-blur" style="border-color: var(--borde); background-color: color-mix(in oklch, var(--superficie) 85%, transparent);">
  <nav class="max-w-5xl mx-auto px-4 sm:px-6 h-14 flex items-center gap-1" aria-label="Navegación principal">
    <a href="/" class="font-bold text-lg mr-4 tracking-tight">
      <span class="text-acento-600 dark:text-acento-300">Coach</span>App
    </a>

    <ul class="flex items-center gap-1 text-sm">
      {
        enlaces.slice(1).map(({ href, texto }) => (
          <li>
            <a
              href={href}
              aria-current={rutaActual === href ? 'page' : undefined}
              class:list={[
                'px-3 py-1.5 rounded-lg transition-colors hover:bg-acento-50 dark:hover:bg-acento-900/40',
                rutaActual === href ? 'text-acento-600 dark:text-acento-300 font-medium' : '',
              ]}
              style={rutaActual === href ? '' : 'color: var(--texto-suave);'}
            >
              {texto}
            </a>
          </li>
        ))
      }
    </ul>
  </nav>
</header>
